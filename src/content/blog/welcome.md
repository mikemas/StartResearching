---
title: "Welcome — Squarespace migration starter"
pubDate: 2026-09-12
description: "Placeholder post proving the blog collection works. Replace with Squarespace import."
canonicalUrl: "https://www.startresearching.com/"
---

This is a placeholder. Run the importer to pull live content:

```bash
npm run import:squarespace
```

Inventory of live URLs is in `content-inventory.txt` (205 URLs from `sitemap.xml`).

Next: convert Squarespace blog JSON / RSS into `src/content/blog/*.md` with frontmatter
`title`, `pubDate`, `description`, `canonicalUrl`.
