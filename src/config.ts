// Site services config. Empty IDs render the slot as an HTML comment until
// Sherri provides the value — nothing breaks, functionality just activates.

/** Disqus shortname (Sherri creates at disqus.com, Sites → Settings). */
export const DISQUS_SHORTNAME = "";

/** Formspree form IDs (formspree.io, one form each, confirm her email). */
export const FORMSPREE_CONTACT_ID = "";
export const FORMSPREE_JOBS_ID = "";

/** Donate button target (Stripe payment link recommended). Empty hides it. */
export const DONATE_URL = "";

/** Advertising + analytics IDs (ported from the live site). */
export const ADSENSE_CLIENT = "ca-pub-8442952758105071";
export const MEDIANET_CID = "8CUEF9XKU";
export const GA_ID = "G-Z559SPN1JF";
